---
name: Bug report
about: Report a problem with minicode
labels: bug
---

**What happened?**
A clear description of the problem.

**What did you expect to happen?**

**Steps to reproduce**
1. Run `minicode ...`
2. ...
3. ...

**Environment (please complete):**
- OS / arch (e.g. Debian 12 / armv7l, Windows 11 / x64):
- Node version (`node -v`):
- minicode version (`minicode --help` header):
- Model / base URL shape (e.g. `https://api.deepseek.com/v1`, `http://localhost:11434/v1`) — **never paste your API key**:

**Full terminal output**
```
paste here
```

**Additional context**
Anything else that might help (sessions are in `~/.minicode/sessions/`, JSON, share the relevant one with keys redacted).
